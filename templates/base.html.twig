<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %} - Jobiz</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            {{ importmap('app') }}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-gray-50 min-h-screen">
        <nav class="bg-white shadow-sm">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex">
                        <div class="flex-shrink-0 flex items-center">
                            <img class="h-8 w-auto" src="{{ asset('images/logo-jobiz.png') }}" alt="Jobiz">
                        </div>
                        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                            <a href="{{ path('app_home') }}" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                                Home
                            </a>
                            <a href="{{ path('app_jobs') }}" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                                Jobs
                            </a>
                            <a href="{{ path('app_about') }}" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                                About
                            </a>
                        </div>
                    </div>
                    <div class="hidden sm:ml-6 sm:flex sm:items-center">
                        {% if app.user %}
                            <div class="ml-3 relative">
                                <span class="text-gray-700">{{ app.user.email }}</span>
                                <a href="{{ path('app_logout') }}" class="ml-4 text-gray-500 hover:text-gray-700">Logout</a>
                            </div>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="text-gray-500 hover:text-gray-700">Login</a>
                            <a href="{{ path('app_register') }}" class="ml-4 btn btn-primary">Sign up</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>

        <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            {% for message in app.flashes('success') %}
                <div class="mb-4 p-4 bg-green-100 text-green-700 rounded-lg">
                    {{ message }}
                </div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="mb-4 p-4 bg-red-100 text-red-700 rounded-lg">
                    {{ message }}
                </div>
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

        <footer class="bg-white mt-12 border-t border-gray-200">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <div>
                        <h3 class="text-sm font-semibold text-gray-400 tracking-wider uppercase">About</h3>
                        <p class="mt-4 text-base text-gray-500">
                            Find your dream job with Jobiz. We connect talented professionals with amazing companies.
                        </p>
                    </div>
                    <div>
                        <h3 class="text-sm font-semibold text-gray-400 tracking-wider uppercase">Quick Links</h3>
                        <ul class="mt-4 space-y-4">
                            <li>
                                <a href="{{ path('app_home') }}" class="text-base text-gray-500 hover:text-gray-900">
                                    Home
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('app_jobs') }}" class="text-base text-gray-500 hover:text-gray-900">
                                    Jobs
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('app_about') }}" class="text-base text-gray-500 hover:text-gray-900">
                                    About
                                </a>
                            </li>
                        </ul>
                    </div>
                    <div>
                        <h3 class="text-sm font-semibold text-gray-400 tracking-wider uppercase">Contact</h3>
                        <ul class="mt-4 space-y-4">
                            <li class="text-base text-gray-500">
                                Email: contact@jobiz.com
                            </li>
                            <li class="text-base text-gray-500">
                                Phone: (555) 123-4567
                            </li>
                        </ul>
                    </div>
                </div>
                <div class="mt-8 border-t border-gray-200 pt-8">
                    <p class="text-base text-gray-400 text-center">
                        © {{ "now"|date("Y") }} Jobiz. All rights reserved.
                    </p>
                </div>
            </div>
        </footer>
    </body>
</html>